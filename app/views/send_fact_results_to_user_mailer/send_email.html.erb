<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <br>
    <h1 class="display-4"><%= @email %></h1>
    <% if @data.any? %>
      <% @data.each do |result| %>
        <div id="<%= dom_id @data.last.search %>" class="card">
          <div class="card-header">
            <strong><%= I18n.t('activerecord.attributes.search.query') %>:</strong>  <%= result.search.query.capitalize %>
          </div>
          
          <div class="card-body">
            <blockquote class="blockquote mb-0">
              <p><%= result.value %></p>
              <footer class="blockquote-footer">
              <% if result.categories.any? %>
                <%= result.categories.map(&:name).map(&:capitalize).join(',') %></footer>
              <% end %>
            </blockquote>
          </div>
        </div>
        <br>
      <% end %>
    <% else %>
      <p class="lead"><%= I18n.t('messages.without_results') %></p>
    <% end %>
  </body>
</html>

